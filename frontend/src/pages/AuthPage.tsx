const AuthPage = (): JSX.Element => (
  <main className="page-shell">
    <section className="card">
      <h1>Sign in to ContractSense</h1>
      <p>TODO: add login and registration forms backed by `/api/auth`.</p>
    </section>
  </main>
);

export default AuthPage;

